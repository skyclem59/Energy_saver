<div class="col-md-4">
  <div class="card card-nav-tabs">
    <div class="card-header card-header-primary">
      <h3><%= "Lumière" %></h3>
    </div>
    <div class="card-body light align">
      <% @light_devices.each do |device| %>
      <%= link_to "#" do %>
      <div class="single-light">
        <%= device.name %>
      </div>
      <button id="<%= device.name.delete(' ') %>" type="button" class="btn btn-primary btn-round"></button>
      <% end %>
      <% end %>
    </div>
  </div>
</div>
